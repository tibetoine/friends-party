version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node@3.0.0
    steps:
      - run: echo "this is the build job"
# Orchestrate our job run sequence
workflows:
  build_and_test:
    jobs:
      - build
