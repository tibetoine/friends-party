version: 2.1

executors:
  default-executor:
    docker:
      - image: cypress/base:10
jobs:
  build:
    executor: default-executor
    steps:
      - run:
          name: 'Build'
          command: npm run build
# Orchestrate our job run sequence
workflows:
  build_and_test:
    jobs:
      - build
